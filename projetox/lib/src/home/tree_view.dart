import 'package:flutter/material.dart';

class TreeView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Tree'),
      ),
      body: const Center(
        child: Text('Tela Tree'),
      ),
    );
  }
}
